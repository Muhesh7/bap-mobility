const { search, onSearch } = require('./mobility/search')
const { select, onSelect } = require('./mobility/select')

const Controllers = {
    search, onSearch,
    select, onSelect
}

module.exports = Controllers